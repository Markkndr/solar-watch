server:
  port: 8080

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/solar_watch}
    username: ${SPRING_DATASOURCE_USERNAME:solar}
    password: ${SPRING_DATASOURCE_PASSWORD:solar}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  jackson:
    serialization:
      write-dates-as-timestamps: false

app:
  cors:
    allowed-origin: ${APP_CORS_ALLOWED_ORIGIN:http://localhost:5173}
  jwt:
    secret: ${APP_JWT_SECRET:VGhpc0lzQVN1ZmZpY2llbnRseUxvbmdCYXNlNjRTZWNyZXRGb3JKV1QxMjM0NTY3ODkw}
    expiration-ms: ${APP_JWT_EXPIRATION_MS:86400000}
  api:
    open-meteo-geocoding-url: ${APP_GEOCODING_URL:https://geocoding-api.open-meteo.com/v1/search}
    sunrise-sunset-url: ${APP_SUNRISE_SUNSET_URL:https://api.sunrise-sunset.org/json}
